services:
  bot:
    # Reemplazá <owner> por tu usuario/organización de GitHub (en minúsculas).
    image: ghcr.io/androettop/discord-spam-bot:latest
    container_name: discord-spam-bot
    env_file: .env
    restart: unless-stopped
