services:
  bot:
    # Replace the owner with your GitHub username/org (lowercase).
    image: ghcr.io/androettop/discord-spam-bot:latest
    container_name: discord-spam-bot
    env_file: .env
    restart: unless-stopped
